- provide(:title, @room_type.name)
%ul
  %li= @room_type.name
  %li= @room_type.spaces
  %li= number_to_currency(@room_type.price, precision: 2, :locale => "es-PE")

= link_to "Back", :back, class: "btn btn-large"