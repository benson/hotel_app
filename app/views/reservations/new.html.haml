- provide(:title, "Creating Reservation") 
%h1 New Reservation

.row
  .span6.offset3
    = form_for @reservation do |f| 
      = render 'shared/error_messages', object: f.object
      = f.label :start_date
      = f.text_field :start_date 
      = f.label :end_date
      = f.text_field :end_date
      = f.label :company
      = f.collection_select(:company_ids, Company.all, :id, :name, {include_blank: true})
      = f.label :customer
      = f.collection_select(:customer_ids, Customer.all, :id, :first_name)
      = f.label :room
      = f.collection_select(:room_ids, Room.all, :id, :name)

      = f.submit "Create reservation!", class: "btn btn-large btn-primary"
      = link_to "Back", :back, class: "btn btn-large pull-right"